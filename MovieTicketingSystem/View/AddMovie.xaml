<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="MovieTicketingSystem.View.AddMovie"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:MovieTicketingSystem.View"
    xmlns:vm="clr-namespace:MovieTicketingSystem.ViewModel"
    x:DataType="vm:AddMovieViewModel">

    <ContentPage.Resources>
        <ResourceDictionary>
            <Style TargetType="Label">
                <Setter Property="VerticalTextAlignment" Value="Center" />
                <Setter Property="WidthRequest" Value="200" />
            </Style>

            <Style TargetType="Entry">
                <Setter Property="WidthRequest" Value="300" />
                <Setter Property="BackgroundColor" Value="#262a2b" />
            </Style>

            <Style TargetType="Image">
                <Setter Property="WidthRequest" Value="300" />
            </Style>
            <Style TargetType="Border">
                <Setter Property="Stroke" Value="White" />
            </Style>
            <Style TargetType="Button">
                <Setter Property="WidthRequest" Value="200" />
                <Setter Property="HeightRequest" Value="50" />
                <Setter Property="Background" Value="#cc4f46" />
                <Setter Property="TextColor" Value="White" />
                <Setter Property="FontAttributes" Value="Bold" />
            </Style>

        </ResourceDictionary>
    </ContentPage.Resources>

    <!--  Main Layout  -->
    <!--  Make entries wrap with border for Movie Properties  -->
    <Frame BackgroundColor="Black" BorderColor="Black">
        <VerticalStackLayout >
            <Image Source="logoww.png" HorizontalOptions="Start" WidthRequest="350"/>
            <HorizontalStackLayout HorizontalOptions="Center" Spacing="10" Margin="0,20,0,0">
                <Frame WidthRequest="500" HeightRequest="550" BorderColor="Gray" BackgroundColor="Black" CornerRadius="25">
                    <VerticalStackLayout Spacing="5">
                        <HorizontalStackLayout>
                            <Label Text="Title" />
                            <Border Margin="-70,0,0,0">
                                <Border.StrokeShape>
                                    <RoundRectangle CornerRadius="10,10,10,10" /> 
                                </Border.StrokeShape>
                                <Entry Text="{Binding Movie.Name}" />
                            </Border>
                        </HorizontalStackLayout>
                        <HorizontalStackLayout>
                            <Label Text="Company Source" />
                            <Border Margin="-70,0,0,0">
                                <Border.StrokeShape>
                                    <RoundRectangle CornerRadius="10,10,10,10" />
                                </Border.StrokeShape>
                                <Entry Text="{Binding Movie.CompanySource}" />
                            </Border>
                        </HorizontalStackLayout>
                        <HorizontalStackLayout>
                            <Label Text="Price" />
                            <Border Margin="-70,0,0,0">
                                <Border.StrokeShape>
                                    <RoundRectangle CornerRadius="10,10,10,10" />
                                </Border.StrokeShape>
                                <Entry Text="{Binding Movie.Price}" />
                            </Border>
                        </HorizontalStackLayout>
                        <HorizontalStackLayout>
                            <Label Text="Description" />
                            <Border Margin="-70,0,0,0">
                                <Border.StrokeShape>
                                    <RoundRectangle CornerRadius="10,10,10,10" />
                                </Border.StrokeShape>
                                <Entry Text="{Binding Movie.Description}" />
                            </Border>
                        </HorizontalStackLayout>
                        <HorizontalStackLayout>
                            <Label Text="Duration" />
                            <Border  Margin="-70,0,0,0">
                                <Border.StrokeShape>
                                    <RoundRectangle CornerRadius="10,10,10,10" />
                                </Border.StrokeShape>
                                <TimePicker Format="T" Time="{Binding Movie.Duration}" />
                            </Border>
                        </HorizontalStackLayout>
                        <HorizontalStackLayout>
                            <Label Text="Year Released" />
                            <Border  Margin="-70,0,0,0">
                                <Border.StrokeShape>
                                    <RoundRectangle CornerRadius="10,10,10,10" />
                                </Border.StrokeShape>
                                <DatePicker Date="{Binding Movie.YearReleased}" />
                            </Border>
                        </HorizontalStackLayout>
                        <HorizontalStackLayout>
                            <Label Text="Start Date" />
                            <Border  Margin="-70,0,0,0">
                                <Border.StrokeShape>
                                    <RoundRectangle CornerRadius="10,10,10,10" />
                                </Border.StrokeShape>
                                <DatePicker Date="{Binding Movie.StartDate}" />
                            </Border>
                        </HorizontalStackLayout>
                        <HorizontalStackLayout>
                            <Label Text="End Date" />
                            <Border  Margin="-70,0,0,0">
                                <Border.StrokeShape>
                                    <RoundRectangle CornerRadius="10,10,10,10" />
                                </Border.StrokeShape>
                                <DatePicker Date="{Binding Movie.EndDate}" />
                            </Border>
                        </HorizontalStackLayout>
                        <HorizontalStackLayout Margin="120,0,0,0">
                            <RadioButton Content="Upcoming" IsChecked="{Binding Movie.IsUpcoming}" />
                            <RadioButton Content="Now Showing" IsChecked="{Binding Movie.IsNowShowing}" />
                        </HorizontalStackLayout>
                        <HorizontalStackLayout Margin="40,0,0,0" Spacing="5">
                            <Button Command="{Binding SaveCommand}" Text="Save Details" CornerRadius="25" WidthRequest="180"/>
                            <Button Command="{Binding .}" Text="Reset"  CornerRadius="25"  WidthRequest="180"/>
                        </HorizontalStackLayout>
                    </VerticalStackLayout>
                </Frame>

                <Frame WidthRequest="500" HeightRequest="550" BorderColor="Gray" BackgroundColor="Black" CornerRadius="25">
                    <VerticalStackLayout>
                        <HorizontalStackLayout HorizontalOptions="Center">
                            <Label Text="Genre" Margin="100,0,50,10"/>
                            <Label Text="Subtitles" Margin="-30,0,0,0"/>
                        </HorizontalStackLayout>
                        <HorizontalStackLayout Margin="70,10,0,0">
                            <VerticalStackLayout>
                                <CollectionView HorizontalOptions="Center" ItemsSource="{Binding AvailableGenre}">
                                    <CollectionView.ItemTemplate>
                                        <DataTemplate>
                                            <Grid ColumnDefinitions="*,*">
                                                <CheckBox />
                                                <Label Grid.Column="1" Text="{Binding .}" />
                                            </Grid>
                                        </DataTemplate>
                                    </CollectionView.ItemTemplate>
                                </CollectionView>
                            </VerticalStackLayout>
                            
                            <VerticalStackLayout Margin="50,0,0,0">
                                <CollectionView HorizontalOptions="Center" ItemsSource="{Binding AvailableSubtitles}">

                                    <CollectionView.ItemTemplate>
                                        <DataTemplate>
                                            <Grid ColumnDefinitions="*,*">
                                                <CheckBox />
                                                <Label Grid.Column="1" Text="{Binding .}" />
                                            </Grid>
                                        </DataTemplate>
                                    </CollectionView.ItemTemplate>
                                </CollectionView>
                            </VerticalStackLayout>
                        </HorizontalStackLayout>
                        
                    </VerticalStackLayout>
                    
                </Frame>
            </HorizontalStackLayout>
        </VerticalStackLayout>
    </Frame>
</ContentPage>