<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="MovieTicketingSystem.View.Customer"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:sys="clr-namespace:System;assembly=netstandard"
    xmlns:converter="clr-namespace:MovieTicketingSystem.Converters"
    xmlns:model="clr-namespace:MovieTicketingSystem.Model"
    xmlns:vm="clr-namespace:MovieTicketingSystem.ViewModel"
    Title="Customer"
    x:DataType="vm:CustomerViewModel">

    <ContentPage.Resources>
        <ResourceDictionary>
            <converter:ByteArrayToImageSourceConverter x:Key="ImageConverter" />
        </ResourceDictionary>
    </ContentPage.Resources>

    <ScrollView>
        <Frame BackgroundColor="#1D1B1B" BorderColor="#1D1B1B">
            <VerticalStackLayout>
                <Frame BackgroundColor="#1D1B1B" BorderColor="#1D1B1B">
                    <!--  Top Level date, ticket button and user button  -->
                    <HorizontalStackLayout>
                        <HorizontalStackLayout>
                            <Image
                                Margin="0,-10,0,0"
                                HorizontalOptions="Start"
                                Source="logoww.png"
                                WidthRequest="250" />
                            <Label
                                Margin="0,0,0,0"
                                FontSize="20"
                                Text="|" />
                            <Label
                                Margin="0,0,0,0"
                                FontSize="20"
                                Text="{Binding Source={x:Static sys:DateTime.Today}, StringFormat='{0:d}'}"
                                TextColor="LightGray" />
                        </HorizontalStackLayout>
                        <HorizontalStackLayout Spacing="20">
                            <ImageButton
                                Margin="750,0,0,0"
                                BackgroundColor="#1D1B1B"
                                Source="ticketa.png"
                                WidthRequest="20" />
                            <ImageButton
                                BackgroundColor="#1D1B1B"
                                Source="user.png"
                                WidthRequest="20" />
                        </HorizontalStackLayout>
                    </HorizontalStackLayout>
                </Frame>
                <Frame
                    BackgroundColor="#1D1B1B"
                    BorderColor="DimGrey"
                    HeightRequest="400">
                    <!--  Featured Movie, title and subtitle, genres, year, part, company, buy ticket button  -->
                    <VerticalStackLayout>
                        <HorizontalStackLayout HorizontalOptions="Center">
                            <Frame
                                BackgroundColor="#1D1B1B"
                                HeightRequest="370"
                                WidthRequest="630">
                                <StackLayout HorizontalOptions="Center">
                                    <HorizontalStackLayout HorizontalOptions="Center">
                                        <!--  Collection View sa mga genres ari  -->
                                        <Label Text="Genre1" TextColor="LightGray" />
                                        <Label Text=" | " TextColor="LightGray" />
                                        <Label Text="Genre2" TextColor="LightGray" />
                                        <Label Text=" | " />
                                        <Label Text="Genre2" TextColor="LightGray" />
                                    </HorizontalStackLayout>
                                    <Label
                                        Margin="0,40,0,0"
                                        FontAttributes="Bold"
                                        FontSize="55"
                                        HorizontalOptions="Center"
                                        Text="MAIN TITLE" />
                                    <Label
                                        Margin="0,-10,0,0"
                                        FontAttributes="Bold"
                                        FontSize="Title"
                                        HorizontalOptions="Center"
                                        Text="subtitle" />

                                    <HorizontalStackLayout Margin="0,55,0,0" HorizontalOptions="Center">
                                        <!--  Binding ari, wala lang nako ge butang kay ma bug ig run  -->
                                        <Label Text="2023 (Year)" TextColor="LightGray" />
                                        <Label Text=" | " TextColor="LightGray" />
                                        <Label Text="Sequel (part)" TextColor="LightGray" />
                                        <Label Text=" | " />
                                        <Label Text="Company" TextColor="LightGray" />
                                    </HorizontalStackLayout>

                                    <Button
                                        Margin="0,30,0,0"
                                        BackgroundColor="#EC4D37"
                                        CornerRadius="18"
                                        Text="Buy Ticket"
                                        TextColor="White"
                                        WidthRequest="105" />

                                </StackLayout>
                            </Frame>
                            <Frame
                                BackgroundColor="#1D1B1B"
                                BorderColor="#1D1B1B"
                                HeightRequest="370"
                                WidthRequest="630">
                                <StackLayout>
                                    <Image Aspect="AspectFill" Source="poster1.jpg" />
                                </StackLayout>
                            </Frame>
                        </HorizontalStackLayout>
                    </VerticalStackLayout>
                </Frame>

                <!--  carouselview sa mga movies  -->

                <Label
                    Margin="0,10,0,0"
                    FontSize="Large"
                    Text="NOW SHOWING" />
                <Frame BackgroundColor="#1D1B1B" HeightRequest="300">

                    <CarouselView ItemsSource="{Binding MovieCollection}">
                        <CarouselView.ItemsLayout>
                            <LinearItemsLayout ItemSpacing="10" Orientation="Horizontal" />
                        </CarouselView.ItemsLayout>
                        <CarouselView.ItemTemplate>
                            <DataTemplate x:DataType="model:Movie">
                                <Image>
                                    <!--  only show image when isnowshowing value is true  -->
                                    <Image.Triggers>
                                        <DataTrigger
                                            Binding="{Binding IsNowShowing}"
                                            TargetType="Image"
                                            Value="True">
                                            <Setter Property="Source" Value="{Binding Image, Converter={StaticResource ImageConverter}}" />
                                        </DataTrigger>
                                    </Image.Triggers>
                                    <Image.GestureRecognizers>
                                        <TapGestureRecognizer Command="{Binding Source={RelativeSource AncestorType={x:Type vm:CustomerViewModel}},Path=GoToTicketPageCommand}" CommandParameter="{Binding .}" />
                                    </Image.GestureRecognizers>
                                </Image>
                            </DataTemplate>
                        </CarouselView.ItemTemplate>
                    </CarouselView>
                </Frame>

                <!--  Carouselview for coming soon mvoies  -->

                <Label
                    Margin="0,10,0,0"
                    FontSize="Large"
                    Text="COMING SOON" />
                <Frame BackgroundColor="#1D1B1B" HeightRequest="300">
                    <CarouselView ItemsSource="{Binding MovieCollection}">
                        <CarouselView.ItemsLayout>
                            <LinearItemsLayout ItemSpacing="10" Orientation="Horizontal" />
                        </CarouselView.ItemsLayout>
                        <CarouselView.ItemTemplate>
                            <DataTemplate x:DataType="model:Movie">
                                <Image>
                                    <!--  only show image when isnowshowing value is true  -->
                                    <Image.Triggers>
                                        <DataTrigger
                                            Binding="{Binding IsUpcoming}"
                                            TargetType="Image"
                                            Value="True">
                                            <Setter Property="Source" Value="{Binding Image, Converter={StaticResource ImageConverter}}" />
                                        </DataTrigger>
                                    </Image.Triggers>
                                    <Image.GestureRecognizers>
                                        <TapGestureRecognizer Command="{Binding Source={RelativeSource AncestorType={x:Type vm:CustomerViewModel}},Path=GoToTicketPageCommand}" CommandParameter="{Binding .}" />
                                    </Image.GestureRecognizers>
                                </Image>
                            </DataTemplate>
                        </CarouselView.ItemTemplate>
                    </CarouselView>
                </Frame>
            </VerticalStackLayout>
        </Frame>
    </ScrollView>
</ContentPage>